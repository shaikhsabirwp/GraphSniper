# GraphSniper
GraphSniper is a small recon tool that pulls out GraphQL queries and mutations directly from client-side JavaScript files. It’s useful when introspection is disabled but the frontend still exposes all the GraphQL operations it uses.
